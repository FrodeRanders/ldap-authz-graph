# ldap-authz-graph
Model RBAC as an authorization graph inside LDAP. Provides a lightweight LDAP convention for users, groups, systems, and roles together with a Java API to manage this. Supports system-scoped roles with global groups, implemented as explicit membership entries. 
